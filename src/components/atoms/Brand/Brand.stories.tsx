import React, { ReactElement } from 'react'

import { Brand } from 'components/atoms/Brand'

export default {
  title: 'Components/Brand',
  component: Brand
}

export const usage = (): ReactElement => (
  <div style={{ backgroundColor: 'var(--blue)', padding: '8px' }}>
  
  </div>
)
